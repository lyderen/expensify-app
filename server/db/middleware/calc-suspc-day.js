const mongoose = require('mongoose');
const {Day,DayGuest} =require('../../models/days');

 


// DB call to find a users DaysSus and send back to client to gui to know the next time suspicion
const calcculatNextSus = () => {
   const day = new Day({

   })
   day.find({})
};



